-- Export statement of <%=tab.label%>
-- generated by DataModel Converter (DML Face).
EXPORT TO <%=tab.schema_name%>.<%=tab.name%>.del of del
<% if tab.has_lob_column? %>
	LOBS TO ./LOBS
--	MODIFIED BY LOBSINSEPFILES
<% end %>
SELECT
	<%= tab.columns.collect{|col| col.name}.join(",\n\t")%>
FROM
	<%=tab.schema_name%>.<%=tab.name%>
;
