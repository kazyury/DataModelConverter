-- refresh identity statement of <%=tab.label%>
-- generated by DataModel Converter (DML Face).
<% tab.identity_columns.each do |x| %>
-- <%= tab.label %> のID列：<%= x.label %> のリフレッシュ用
-- SELECT MAX(<%= x.name %>) FROM <%=tab.schema_name%>.<%=tab.name%>;
ALTER TABLE <%=tab.schema_name%>.<%=tab.name%> ALTER COLUMN <%= x.name %> RESTART WITH XXX ;
<% end %>
